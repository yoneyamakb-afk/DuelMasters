#nullable enable

namespace DMRules.Engine;

public partial class MinimalState
{
    /// <summary>
    /// Delegate to the underlying engine State instance.
    /// </summary>
    public void SetPhase(Phase phase)
    {
        // MinimalState holds the concrete State as 'S'.
        S.SetPhase(phase);
    }
}
